import Logo from "@/components/Logo";

const Sidebar = () => {
  return (
    <nav className="w-full min-h-screen max-w-sm border-r-2 border-slate-300">
      <Logo />
    </nav>
  );
};

export default Sidebar;
